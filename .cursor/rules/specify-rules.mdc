---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# felloway_client Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-05-27

## Active Technologies
- C# 12 / .NET 8 + ASP.NET Core health checks (`Microsoft.Extensions.Diagnostics.HealthChecks`), EF Core health check (`Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore`), optional `AspNetCore.HealthChecks.UI.Client` 8.x for `UIResponseWriter` (003-custom-health-checks)
- PostgreSQL via existing `FelloWayDbContext` (readiness = `CanConnect` equivalent via `AddDbContextCheck`) (003-custom-health-checks)
- Bash 5+; .NET 8 (merge tool); Dart 3.10+ / Flutter (consumer) + `Microsoft.OpenApi.Readers`, `YamlDotNet`, `Newtonsoft.Json` (merge tool); `@openapitools/openapi-generator-cli` 7.10.x; Java 11+ (generator JVM); Node.js 18+ (npx) (004-openapi-dart-codegen)
- N/A (build artifacts in repo: `openapi.json`, generated Dart) (004-openapi-dart-codegen)
- Bash 5+; Node.js 18+; Dart 3.10+ / Flutter + `@redocly/cli` (bundle/join); `@openapitools/openapi-generator-cli` 7.10.x (global or npx); Java 11+ (JVM for generator) (004-openapi-dart-codegen)
- Committed `openapi.json` + `frontend/lib/generated/felloway_api/` (004-openapi-dart-codegen)
- Dart 3.10+ / Flutter stable; .NET 8 backend (verification only) + `dio`, `flutter_appauth`, `felloway_api` (path dep, optional mappers), ASP.NET JWT auth (002) (005-api-backend-integration)
- `TokenStorage` (secure prefs) for access/refresh tokens (005-api-backend-integration)
- .NET 8, C# 12, xUnit 2.5 + `Microsoft.AspNetCore.Mvc.Testing`, `Microsoft.EntityFrameworkCore.InMemory`, `Testcontainers.PostgreSql` 4.3, Npgsql, Hangfire.PostgreSql (006-hybrid-test-database)
- InMemory (fast suite); PostgreSQL (integration suite) (006-hybrid-test-database)
- C# 12 / .NET 8 + ASP.NET Core built-in CORS middleware (`AddCors` / `UseCors`) (007-api-cors-policy)
- Configuration `Cors:AllowedOrigins`; Development localhost predicate (007-api-cors-policy)
- Terraform ≥ 1.5; .NET 8; Dart 3.10+ / Flutter stable; GitHub Actions + AWS provider, ECS Fargate, ECR, RDS PostgreSQL 16, ALB, S3, CloudFront, ACM, Route 53, Secrets Manager; `configure-aws-credentials` OIDC (008-aws-deploy-pipeline)
- RDS per env; S3 web buckets; Terraform remote state S3 (008-aws-deploy-pipeline)
- C# 12 / .NET 8; Dart 3.10+ / Flutter stable + `flutter_appauth`, `dio`; ASP.NET Core, EF Core 8, `HttpClient`, Polly (LinkedIn HTTP), existing `AuthService` / JWT (main)
- PostgreSQL (`users`, `oauth_identities`, `refresh_tokens`); optional new `users.email` column (main)
- C# 12 / .NET 8 + `AspNet.Security.OAuth.LinkedIn`, Cookie + JwtBearer auth, `IMemoryCache` mobile tickets; credentialed CORS for web (009-linkedin-bff-auth)
- Dart 3.10+ / Flutter stable + `dio` (withCredentials web), `flutter_web_auth_2` (mobile only), `flutter_secure_storage`; BFF LinkedIn — no client PKCE (009-linkedin-bff-auth)
- PostgreSQL (`users`, `oauth_identities`, `refresh_tokens`); `IMemoryCache` for mobile auth tickets (009-linkedin-bff-auth)
- [e.g., Dart 3.x, Flutter stable channel or NEEDS CLARIFICATION] + [e.g., flutter_bloc, freezed, dio, go_router or NEEDS CLARIFICATION] (009-linkedin-bff-auth)
- [e.g., shared_preferences, hive, sqflite, secure storage, remote API, or N/A] (009-linkedin-bff-auth)
- C# 12 / .NET 8 (API); Dart 3.10+ / Flutter stable (client) + `AspNet.Security.OAuth.LinkedIn` 8.x, `Microsoft.AspNetCore.Authentication.Cookies`, `Microsoft.AspNetCore.Authentication.JwtBearer`, EF Core 8 + Npgsql; Flutter: `dio`, `flutter_web_auth_2` (mobile only), `go_router`, `flutter_secure_storage` (009-linkedin-bff-auth)
- PostgreSQL (`users`, `oauth_identities`, `refresh_tokens`); `IMemoryCache` for `MobileAuthTicket` (ephemeral) (009-linkedin-bff-auth)
- Dart 3.10+ / Flutter stable (`frontend/`, SDK `^3.10.4`) + `flutter` (Material `SnackBar`, `ScaffoldMessenger`), `dio` (error classification only), existing `app_localizations` (010-connectivity-snackbar)
- N/A (ephemeral UI) (010-connectivity-snackbar)
- C# 12 / .NET 8 (API); Dart 3.10+ / Flutter stable + EF Core 8 + Npgsql; ASP.NET Core controllers; OpenAPI merge + `openapi-generator` dart-dio; existing `dio` / `go_router` (011-backend-interests-catalog)
- PostgreSQL `interests`, `user_interests` (existing) (011-backend-interests-catalog)
- N/A (Stream public key in compile-time config + optional `env.json` on web host) (012-chats-stream-connect)
- N/A (compile-time constants in web bundle only) (013-clarify-env-json)
- Dart 3.10+ / Flutter stable + bundled Onest (400/500/600), `ThemeExtension` typography tokens, Material 3 `TextTheme` (014-mobile-typography)
- N/A (static font assets in `frontend/assets/fonts/onest/`) (014-mobile-typography)
- Dart 3.10+ / Flutter stable (`frontend/`, SDK `^3.10.4`) + `dio`, `go_router`, `flutter_appauth` / BFF web auth, `stream_chat`, existing repositories + `AppConfig` (015-remove-mock-local)
- N/A (no client-side mock stores); `TokenStorage` unchanged (015-remove-mock-local)
- C# 12 / .NET 8 + ASP.NET Core, `IOAuthTokenExchanger`, `AuthService`, `OAuthOptions`, LinkedIn BFF (`009-linkedin-bff-auth`) (016-remove-dev-oauth-backend)
- PostgreSQL (unchanged); test InMemory / Testcontainers (016-remove-dev-oauth-backend)
- Dart 3.10+ / Flutter stable (`frontend/`, SDK `^3.10.4`) + `go_router`, `AppTheme` / `FellowayTextColors`, `UsersRepository.getMe()`, `OnboardingPreferences`, existing onboarding pages (017-post-signin-onboarding-contrast)
- `SharedPreferences` (`OnboardingPreferences`); profile from API (017-post-signin-onboarding-contrast)
- C# 12 / .NET 8 (admin web + API extensions); Terraform ≥ 1.5 + ASP.NET Core Razor Pages, cookie auth, `HttpClient`, EF Core (API only), existing `IBlobStorageService`, ALB host rules, ECS Fargate, Secrets Manager, GitHub Actions environments (018-github-env-admin-panel)
- PostgreSQL via API (existing `events` table); blob storage for cover images (018-github-env-admin-panel)

- C# 12 / .NET 8 + ASP.NET Core, EF Core 8 + Npgsql, Swashbuckle, FluentValidation, Stream Chat .NET SDK, Azure.Storage.Blobs, Hangfire (PostgreSQL storage), Polly (HTTP resilience for OAuth) (002-backend-api)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

# Add commands for C# 12 / .NET 8

## Code Style

C# 12 / .NET 8: Follow standard conventions

## Recent Changes
- 018-github-env-admin-panel: Added C# 12 / .NET 8 (admin web + API extensions); Terraform ≥ 1.5 + ASP.NET Core Razor Pages, cookie auth, `HttpClient`, EF Core (API only), existing `IBlobStorageService`, ALB host rules, ECS Fargate, Secrets Manager, GitHub Actions environments
- 017-post-signin-onboarding-contrast: Added Dart 3.10+ / Flutter stable (`frontend/`, SDK `^3.10.4`) + `go_router`, `AppTheme` / `FellowayTextColors`, `UsersRepository.getMe()`, `OnboardingPreferences`, existing onboarding pages
- 016-remove-dev-oauth-backend: Added C# 12 / .NET 8 + ASP.NET Core, `IOAuthTokenExchanger`, `AuthService`, `OAuthOptions`, LinkedIn BFF (`009-linkedin-bff-auth`)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
